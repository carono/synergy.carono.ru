<?php

declare(strict_types=1);

namespace Carono\LmsParser;

/**
 * Сессию LMS больше не восстановить: либо обновлять её нечем (нет DISPLAY — значит нет
 * браузера для челленджа DDoS-Guard), либо подряд провалилось слишком много попыток.
 *
 * Бросается вместо тихого возврата false: прогон, который не может обновить сессию,
 * дальше только жжёт время и стучится в LMS впустую. Пусть падает громко и с ненулевым
 * кодом возврата — это видно и человеку, и супервизору.
 */
final class SessionLostException extends \RuntimeException
{
}
